version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - node -v
      - npm -v
      - npm ci
  build:
    commands:
      - npm run build
      - npx aws-cdk --version
      - npx aws-cdk synth

artifacts:
  base-directory: cdk.out
  files:
    - '**/*'